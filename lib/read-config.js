function readConfig(path) {
  // ...
}

function runPreInstallHooks(config) {}

function promptFromConfig(config) {}

function replaceFromConfig(config, dir) {
  // use require('replace')
}

function runPostInstallHooks(config) {}

function deleteTempFiles() {}

// del og cpy fra
